/**
 * Open PWA Backend Api
 */
export const api = {
    emit: ()=>'emit',
    subscribe: () =>'subscribe',
    install: (obj)=> {
        //Evaluate permissions?
        //obj.emit('install',{emit,subscribe})

    }
}