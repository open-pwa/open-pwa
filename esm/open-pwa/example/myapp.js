self.addEventListener('install', ({emit,subscribe}) => {
    // Perform install steps
    // Allows emit of modules or other stuff to install and also allows to obtain information via subscribe.
});


self.addEventListener('remove', ({emit,subscribe}) => {
    // Perform install steps
    // Allows emit of modules or other stuff to install and also allows to obtain information via subscribe.
});

