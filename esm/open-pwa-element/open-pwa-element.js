/**
 * customElement that shows if open-pwa is installed or else let the user supply a open-pwa endpoint
 */
class openPwa extends HTMLElement {
    connectedCallback() {
        //Detect Open PWA
        //connect Open PWA alternate  show dialog
          //Ask for Permission
        //show dialog for open-pwa host

        //List services that are install able and configuration options.
    }
}